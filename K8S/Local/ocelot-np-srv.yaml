apiVersion: v1
kind: Service
metadata:
  name: ocelot-service-nodeport
  labels:
    app: ocelot-api-gateway
spec:
  selector:
    app: ocelot-api-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30818  
  type: NodePort
